{
  spark-options: {
    "key": "value",
    "spark.app.name": "unify"
  },
  cloud-alias : {
    gcp : {
      "gcp.key" : "pass"
    },
    aws : {
      "aws.key" : "pass"
    }
  }

  file-io : {include "file/file_io.conf"}
}